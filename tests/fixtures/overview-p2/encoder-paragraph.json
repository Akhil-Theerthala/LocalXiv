{
  "origin": "Synthetic replay of the recorded verbatim-paragraph repair loop; the raw provider response was not saved.",
  "defect": "a faithful labelled-layer drawing was repaired because the encoder paragraph did not appear verbatim",
  "assignment": {
    "id": "p2",
    "title": "Encoder",
    "purpose": "Show what the encoder produces.",
    "entry_context": [],
    "content": [
      {"text": "The encoder turns the input into two vectors, one per candidate.", "kind": "statement"}
    ],
    "exit_state": "The reader knows the encoder produces two vectors.",
    "shared_facts": {},
    "illustrative_values": [],
    "exact_text": ["0.73 and 0.27"],
    "construction": "mapping"
  },
  "accepted_drawing": ["two labelled layers feed two vectors", "0.73 and 0.27"],
  "rejected_drawing": ["The encoder turns the input into two vectors, one per candidate."],
  "expected": "the semantic paragraph may be expressed visually; only the declared exact text is required"
}
